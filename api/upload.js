import supabase from './db-client.js';import{cors,getUser}from'./auth.js';export default async function handler(req,res){cors(res);if(req.method==='OPTIONS')return res.status(204).end();try{const u=await getUser(req);if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const{fileName,fileBase64,contentType}=req.body;if(!fileBase64)return res.status(400).json({error:'File required'});const safe=`${u.id}/${Date.now()}-${fileName.replace(/[^a-zA-Z0-9._-]/g,'')}`;const{error}=await supabase.storage.from('avatars').upload(safe,Buffer.from(fileBase64,'base64'),{contentType,upsert:true});if(error)throw error;const{data}=supabase.storage.from('avatars').getPublicUrl(safe);return res.json({url:data.publicUrl})}catch(e){return res.status(e.message==='Unauthorized'?401:500).json({error:e.message})}}
