import { AlertCircle,Clock3,LoaderCircle } from 'lucide-react';
import { motion } from 'framer-motion';
export const Card=({children,className=''}:{children:React.ReactNode,className?:string})=><div className={`glass rounded-3xl p-5 sm:p-6 ${className}`}>{children}</div>;
export const PageTitle=({eyebrow,title,desc,action}:{eyebrow:string,title:string,desc:string,action?:React.ReactNode})=><><header className={`${title==='Study Hub'?'mb-0':'mb-7'} flex flex-col justify-between gap-4 sm:flex-row sm:items-end`}><div><p className="mb-2 text-xs font-bold uppercase tracking-[.2em] text-violet-500 dark:text-cyan-300">{eyebrow}</p><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-sm text-slate-500 dark:text-slate-400">{desc}</p></div>{action}</header>{title==='Study Hub'&&<motion.section initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={{duration:.45,ease:'easeOut',delay:.08}} className="mb-5 mt-5 flex w-full items-start gap-4 rounded-2xl border border-white/10 bg-slate-950/80 p-5 shadow-lg shadow-violet-950/10 backdrop-blur-md"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-indigo-600 text-white shadow-lg shadow-violet-500/20"><Clock3 size={19}/></span><div><h2 className="text-lg font-semibold text-white">Why Pomodoro Works for Students</h2><p className="mt-1 text-sm leading-relaxed text-gray-400">The Pomodoro Technique helps students study more effectively by breaking study sessions into short, focused intervals followed by regular breaks. Instead of trying to study for hours without stopping, students work in 25-minute focus sessions and take a 5-minute break before starting the next session.</p></div></motion.section>}</>;
export const Loading=()=> <div className="grid min-h-[45vh] place-items-center"><LoaderCircle className="animate-spin text-violet-500" size={32}/></div>;
export const ErrorBox=({message}:{message:string})=><div className="mb-5 flex gap-2 rounded-xl border border-rose-500/30 bg-rose-500/10 p-3 text-sm text-rose-500"><AlertCircle size={18}/>{message}</div>;
export const Pill=({children,tone='violet'}:{children:React.ReactNode,tone?:string})=><span className={`rounded-full bg-${tone}-500/10 px-2.5 py-1 text-xs font-semibold text-${tone}-500`}>{children}</span>;
