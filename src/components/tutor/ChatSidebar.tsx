import { useMemo,useState } from 'react';import {Menu,MessageSquarePlus,Search,X} from 'lucide-react';import ConversationList from './ConversationList';import type{Conversation}from'./types';
export default function ChatSidebar({open,onClose,items,active,onSelect,onNew,onRename,onDelete,onPin}:{open:boolean;onClose:()=>void;items:Conversation[];active:number|null;onSelect:(id:number)=>void;onNew:()=>void;onRename:(c:Conversation)=>void;onDelete:(c:Conversation)=>void;onPin:(c:Conversation)=>void}){const[q,setQ]=useState('');const filtered=useMemo(()=>items.filter(c=>(c.title+' '+(c.last_preview||'')).toLowerCase().includes(q.toLowerCase())),[items,q]);return <><button onClick={onClose} aria-label="Toggle conversations" className="glass fixed left-20 top-4 z-30 rounded-xl p-2 lg:hidden"><Menu size={19}/></button>{open&&<div onClick={onClose} className="fixed inset-0 z-40 bg-black/55 lg:hidden"/>}<aside className={`absolute inset-y-0 left-0 z-50 flex w-[290px] flex-col border-r border-white/10 bg-slate-950/95 p-3 backdrop-blur-2xl transition-transform lg:relative lg:z-auto lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between px-2 py-3"><b className="text-sm text-white">Conversations</b><button onClick={onClose} className="text-slate-400 lg:hidden"><X size={18}/></button></div><button onClick={onNew} className="btn-primary mb-3 w-full"><MessageSquarePlus size={17}/> New chat</button><label className="relative"><Search className="absolute left-3 top-3 text-slate-500" size={16}/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full rounded-xl border border-white/10 bg-white/5 py-2.5 pl-9 pr-3 text-sm text-white outline-none focus:border-violet-500" placeholder="Search chats" aria-label="Search conversations"/></label><div className="mt-4 flex-1 overflow-y-auto"><ConversationList items={filtered} active={active} onSelect={onSelect} onRename={onRename} onDelete={onDelete} onPin={onPin}/></div></aside></>}
