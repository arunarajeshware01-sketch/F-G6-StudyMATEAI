import {ExternalLink,FileText}from'lucide-react';import type{Source}from'./types';
export default function SourceCard({sources}:{sources?:Source[]}){if(!sources?.length)return null;return <div className="mt-3 rounded-2xl border border-cyan-500/20 bg-cyan-500/5 p-3"><p className="mb-2 text-[10px] font-bold uppercase tracking-widest text-cyan-600">Sources</p><div className="space-y-2">{sources.map((s,i)=><a key={i} href={`${s.url}${s.page?`#page=${s.page}`:''}`} target="_blank" rel="noreferrer" className="flex items-center gap-2 rounded-xl bg-white/50 p-2 text-xs hover:bg-white dark:bg-white/5 dark:hover:bg-white/10"><FileText size={14} className="text-cyan-500"/><span className="flex-1 truncate">{s.name}</span>{s.page&&<span className="text-slate-500">p. {s.page}</span>}<ExternalLink size={11}/></a>)}</div></div>}
