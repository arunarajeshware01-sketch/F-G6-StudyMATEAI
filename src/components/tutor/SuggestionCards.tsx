import {BookOpen,FileQuestion,FileText,GraduationCap,ListChecks,Workflow}from'lucide-react';const prompts=[['Explain today’s lesson',BookOpen],['Quiz me',FileQuestion],['Summarize my notes',FileText],['Help with homework',GraduationCap],['Explain uploaded PDF',ListChecks],['Teach me step by step',Workflow]];
export default function SuggestionCards({onPick}:{onPick:(x:string)=>void}){return <div className="mx-auto mt-10 max-w-2xl text-center"><div className="mx-auto mb-4 grid h-14 w-14 place-items-center rounded-2xl bg-gradient-to-br from-violet-600 to-cyan-500 text-white shadow-xl"><BookOpen/></div><h2 className="text-2xl font-bold">What are we learning today?</h2><p className="mt-2 text-sm text-slate-500">Ask a question, upload your notes, or choose a starting point.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{prompts.map(([text,Icon]:any)=><button key={text} onClick={()=>onPick(text)} className="glass flex items-center gap-3 rounded-2xl p-4 text-left text-sm font-semibold hover:border-violet-500/30"><Icon size={18} className="text-violet-500"/>{text}</button>)}</div></div>}
