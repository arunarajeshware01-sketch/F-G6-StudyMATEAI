import { Pause,Play,SkipBack,SkipForward,X } from 'lucide-react';import { useMusic } from '../contexts/MusicContext';
export default function MiniPlayer(){const{current,playing,toggle,next,previous,progress,duration,seek}=useMusic();if(!current)return null;return <div className="fixed bottom-5 left-4 right-4 z-50 mx-auto max-w-xl rounded-2xl border border-white/10 bg-slate-950/90 p-3 text-white shadow-2xl backdrop-blur-2xl lg:left-[19rem]"><div className="flex items-center gap-3"><img src={current.cover_url} className={`h-11 w-11 rounded-xl object-cover ${playing?'spin-slow':''}`} alt=""/><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{current.title}</p><p className="truncate text-xs text-slate-400">{current.artist}</p></div><button onClick={previous}><SkipBack size={17}/></button><button onClick={toggle} className="grid h-9 w-9 place-items-center rounded-full bg-white text-slate-950">{playing?<Pause size={17}/>:<Play size={17}/>}</button><button onClick={next}><SkipForward size={17}/></button></div><input type="range" min="0" max={duration||100} value={progress} onChange={e=>seek(+e.target.value)} className="mt-2 h-1 w-full accent-violet-500"/></div>}
